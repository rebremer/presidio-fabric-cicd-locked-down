# Conda-side deps only. PyPI deps are vendored as wheels into
# Libraries/CustomLibraries/ at pipeline build time -- see
# requirements-presidio.txt and the "Vendor Presidio wheels" step in
# azure-pipelines.yml. The DEP workspace cannot reach pypi.org during publish.
dependencies:
  - python=3.11
  - pip
