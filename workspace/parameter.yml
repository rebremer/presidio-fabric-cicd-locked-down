# fabric-cicd parameter file (https://microsoft.github.io/fabric-cicd/1.0.0/how_to/parameterization/)
#
# Intentionally empty: this repo targets a single fixed workspace+environment,
# and the notebook's environment binding is hardcoded in
#   workspace/16_presidio/PresidioSmokeTest.Notebook/notebook-content.py
# (the `# META "environmentId" / "workspaceId"` block).
#
# Re-introduce find_replace entries here if this deployment ever needs to
# move between environments (e.g. dev/prod workspaces).
find_replace: []
