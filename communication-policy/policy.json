{
  "_comment": [
    "Workspace communication policy for a WSPL/DEP-locked-down Fabric workspace.",
    "Applied by communication-policy/deploy.py via PUT to:",
    "  {FABRIC_ADMIN_API_URL}/v1/workspaces/{FABRIC_WORKSPACE_ID}/networking/communicationPolicy",
    "",
    "Schema (as returned by GET on the same endpoint):",
    "  inbound.publicAccessRules.defaultAction  -> Deny = WSPL (no public inbound)",
    "  outbound.publicAccessRules.defaultAction -> Deny = DEP (no public outbound)",
    "",
    "Add explicit allow rules under inbound/outbound per the Fabric REST docs",
    "for this endpoint once the rule sub-schema is confirmed for your tenant."
  ],
  "inbound": {
    "publicAccessRules": {
      "defaultAction": "Deny"
    }
  },
  "outbound": {
    "publicAccessRules": {
      "defaultAction": "Deny"
    }
  }
}
