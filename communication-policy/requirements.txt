# communication-policy/deploy.py has no third-party dependencies.
# It uses Python stdlib (urllib) and shells out to the Azure CLI for auth,
# so it runs under the restrictive agent NSG without needing PyPI egress.
