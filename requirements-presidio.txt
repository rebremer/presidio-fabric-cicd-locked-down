# Presidio runtime deps shipped as wheels into the Fabric Environment's
# CustomLibraries/ folder. Pipeline runs `pip download` (full transitive
# closure) against pypi.org from the self-hosted Linux agent, then
# fabric-cicd uploads them to the WSPL workspace.
#
# environment.yml is uploaded then DELETED from staging before publish
# (see deploy.py monkey-patch) -- WSPL blocks pypi/conda-forge so the
# Fabric publish backend must skip the public-deps resolve step.

# Core Presidio
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355

# spaCy small English model (~13 MB)
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
