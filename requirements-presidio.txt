# MINIMAL DIAGNOSTIC: only presidio-anonymizer (small dep tree).
# If this publish succeeds, the failure is in presidio-analyzer/spaCy chain.
# If it fails too, it's a fundamental wheel/manifest issue.
presidio-anonymizer==2.2.355

