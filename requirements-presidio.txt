# Presidio runtime deps shipped as wheels into the Fabric Environment's
# CustomLibraries/ folder. The pipeline step "Vendor Presidio wheels" runs
# `pip download -r requirements-presidio.txt` (full transitive closure)
# against pypi.org from the self-hosted Linux agent, then fabric-cicd
# uploads them to the DEP workspace where pypi.org is unreachable.
#
# environment.yml MUST declare no public deps (python/pip) -- the Fabric
# publish backend would try to resolve them from conda-forge, which WSPL
# blocks. Fabric runtime 1.3 already provides Python 3.11 + pip.

# Core Presidio
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355

# spaCy small English model (~13 MB).
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

