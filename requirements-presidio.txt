# Presidio runtime deps shipped as wheels into the Fabric Environment's
# CustomLibraries/ folder. The pipeline step "Vendor Presidio wheels" runs
# `pip download -r requirements-presidio.txt` against pypi.org from the
# self-hosted agent (which has internet), then fabric-cicd uploads them to
# the DEP workspace where pypi.org is unreachable.
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.5
# Pin transitives to versions that are known to coexist with Fabric runtime
# 1.3 (Spark 3.5 / Python 3.11). Without these pins, pip resolves "latest"
# and the publish fails with cryptic library install errors when newer
# pydantic/numpy/typer collide with the runtime image.
pydantic==2.8.2
pydantic-core==2.20.1
typer==0.12.5
click==8.1.7
numpy==1.26.4
requests==2.32.3
azure-core==1.30.2
phonenumbers==8.13.43
tldextract==5.1.2
regex==2024.5.15
# spaCy small English model (~13 MB) -- the lg variant exceeds the Fabric
# updateDefinition payload limit and fails publish with
# "Array dimensions exceeded supported range".
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
